{
  "name": "programaker-addon",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "description": "Companion addon for programaker.",
  "main": "background.js",
  "scripts": {
    "build": "tsc-bundle --project tsconfig_background.json && tsc-bundle --project tsconfig_popup.json && tsc-bundle --project tsconfig_injected.json",
    "lint": "tslint --project --project tsconfig_background.json && tslint --project tsconfig_popup.json && tslint --project tsconfig_injected.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "devDependencies": {
    "tsc": "^1.20150623.0",
    "tslint": "^5.14.0",
    "typescript": "^3.3.4000",
    "typescript-bundle": "^1.0.12"
  },
  "dependencies": {}
}
